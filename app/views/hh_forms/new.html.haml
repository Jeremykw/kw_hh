.container
	= form_for @hhform, :remote => true do |f|
		=render partial: 'new_title', locals: {f: f}
		=render partial: 'new_contact', locals: {f: f}
		=render partial: 'new_complaints', locals: {f: f}
		=render partial: 'new_checkboxes', locals: {f: f}
		=render partial: 'new_pains', locals: {f: f}
		=render partial: 'new_other', locals: {f: f}
		=render partial: 'new_concent', locals: {f: f}
